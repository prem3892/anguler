import { Component, signal } from '@angular/core';
import { Data } from '../data/data';

@Component({
  selector: 'app-footer',
  imports: [Data],
  templateUrl: './footer.html',
  styleUrl: './footer.css'
})
export class Footer {

}
